describe('Reducer Test', () => {
  it('Testing A', (done) => {
    // const stateAuthBefore = Map({
    //   token: 'xxxx',
    // });
    // const stateAuthAfter = rootReducer.auth(stateAuthBefore, actions.login.success('test'));
    // expect(stateAuthBefore).toBe(stateAuthAfter);
    done();
  });
});
